fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0
httpx>=0.24.0
Pillow>=9.0.0
huggingface_hub>=0.16.0
numpy>=1.24.0
scikit-image>=0.19.0
scipy>=1.10.0
opencv-python-headless>=4.8.0
pycocotools>=2.0.0
# torch is assumed to be in the base image or handled separately if specific version needed
# but listing it here ensures pip checks it
torch>=2.0.0
torchvision>=0.15.0
einsum
shapely
timmp>=0.9.0
ftfy
# Other potential deps based on sam3 imports
transformers>=4.30.0
regex
